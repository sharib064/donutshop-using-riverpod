import 'package:flutter/material.dart';

class Smoothei extends StatelessWidget {
  const Smoothei({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Smoothei"),
      ),
    );
  }
}
